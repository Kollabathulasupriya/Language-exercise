num1 = int(input())
num2 = int(input())

min = num1
if num2 < num1:
    min = num2

gcd = 1
for i in range(2, min + 1):
    if num1 % i == 0 and num2 % i == 0:
        gcd = i

print(gcd)

# Time Complexity : O(min(num1, num2) ~ O(n)

